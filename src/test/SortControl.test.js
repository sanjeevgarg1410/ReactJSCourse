import React from 'react';
import { render, fireEvent , screen,} from '@testing-library/react'

import SortControl from '../components/SortControl';

describe('SortControl', () => {
    it('should change sorting option when a different option is selected', () => {
        // render(<SortControl currentSelection="releaseDate" onSortChange={() => {}} />);

        // const sortSelect = screen.getByTestId('combobox');
  
        
        // // Confirm the initial selection (if needed)
        // expect(sortSelect).toHaveValue('releaseDate');
      
    

        // fireEvent.change(sortSelect, { target: { value: 'title' } });

        
        // // Confirm the change
        // expect(sortSelect).toHaveValue('title');
    });

  });
  